# CODEOWNERS — require review for security-sensitive changes.
#
# GitHub auto-requests these owners on any PR touching the paths below. To make
# their approval *required* (block merge without it), enable branch protection /
# a ruleset on the default branch with "Require review from Code Owners".
#
# Gated by the @mozilla-platform-ops/relops team. PREREQUISITE: that team must
# have WRITE access to this repo, or GitHub silently ignores these rules. Grant
# it via Settings -> Collaborators and teams -> Add teams -> relops -> Write
# (or: gh api -X PUT orgs/mozilla-platform-ops/teams/relops/repos/mozilla-platform-ops/hangar -f permission=push).

# The CODEOWNERS file itself — so the protection can't be quietly removed.
/.github/CODEOWNERS                  @mozilla-platform-ops/relops

# The reprovision authorization allowlist (who may trigger a destructive EACS
# wipe/rebuild) and its enforcement. Changing who can reprovision must be
# reviewed by the relops team.
/backend/app/reprovision_access.py   @mozilla-platform-ops/relops
/backend/app/api/reprovision.py      @mozilla-platform-ops/relops
